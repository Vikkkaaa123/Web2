<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /Web2/

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_URI} !=/favicon.ico
  RewriteRule ^(.*)$ project/index.php?q=$1 [L,QSA]
</IfModule>
